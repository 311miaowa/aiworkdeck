---
alwaysApply: true
---
所有前端页面必须严格遵守《页面设计规范》和既有组件用法，不自行发明样式和布局。

定义任何字段/数据结构前，先全局搜索现有类型/接口/表结构，能复用的坚决复用，不新增同义字段。

所有可配置项（选项、标签、枚举、展示文案）集中在配置文件/常量中维护，禁止在组件或接口里硬编码。

页面应尽量“配置+数据驱动”，业务数据和选项原则上都从后台读取，前端只做展示和交互。

所有数据请求必须通过统一的 API 封装层（如 services/api），禁止在组件中直接写 URL 或散落 fetch/axios。

每次开发业务功能都要前端 + 后端 + 数据库端到端打通，禁止依赖长期存在的假数据或临时 Mock 接口。

后端接口设计必须与领域模型和数据库结构对齐，入参/出参有清晰类型定义并符合统一接口规范。

模块和页面要高内聚、低耦合，实现新需求时优先在本模块内扩展，避免改动无关模块影响其他功能。

任何可复用的 UI 区块和业务逻辑优先抽成组件 / Hook / 服务，严禁复制粘贴式开发。

提交代码前自查：删除调试代码和 TODO，保证命名清晰、类型完整，并通过格式化和静态检查。